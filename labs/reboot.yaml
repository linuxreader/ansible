---
- name: reboot lab hosts
  hosts: practice
  tasks:
    - name: reboot hosts
      reboot:
        msg: Reboot initiated
        test_command: whoami
    - name: print message to show host is back
      debug:
        msg: reboot success
